all:
  children:
    amazon:
      hosts:
%{ for idx, ip in all_public_ips ~}
        ${host_prefix}${idx}:
          ansible_host: ${ip}
%{ endfor ~}
      vars:
        ansible_user: ${ansible_user}
        ansible_ssh_private_key_file: ${ansible_ssh_private_key_file}
        ansible_ssh_common_args: "${ansible_ssh_common_args}"
